spring: 
  application:
    name: quality-audit-service
  cloud:
    config:
      name: quality-audit-service
      discovery:
        service-id: config
        enabled: true

server:
  servlet-path: /qa/*

---
spring:
  profiles: local


---
spring:
  profiles: dev
  cloud:
    config:
      discovery:
        enabled: false
      uri: http://config.caliber-2-dev

server:
  port: 80

eureka:
  client:
    enabled: false
---
spring:
  profiles: prod

